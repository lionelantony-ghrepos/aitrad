# CLI apply copies

Numbered sources of truth: `insforge/migrations/`. This folder exists because `@insforge/cli db migrations` only reads timestamped files here. Keep the SQL body identical to the matching `000N_*.sql` file.
