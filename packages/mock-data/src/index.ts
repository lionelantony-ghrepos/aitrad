export const packageName = "@meridian/mock-data" as const;

/** Seed/universe generators live here (doc 06). Static JSON remains in /mock_data. */
export function generatorsReady(): boolean {
  return true;
}
