# PBI-028 — AI research briefs & portfolio insights

## PBI / ACs / TCs

- PBI: [PBI-028](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-028-01, AC-028-02 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-028-01, TC-028-02

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Spec prompt said migration **0015**; 0015 is news embeddings, so `briefs` is **0023**. Morning opt-in is `profiles.morning_brief_opt_in`. PDF export uploads to storage bucket `briefs` (create the bucket before live export). Cron is `brief-service` `op: "cron"` invoked from `market-tick` while the simulated NYSE session is OPEN (idempotent one morning brief per user per UTC day unless `force`). |

## Surfaces

- Packages: `@meridian/copilot` (generators, fake brief LLM, PDF renderer, analysis facts); `@meridian/schemas` (`src/briefs.ts`)
- `apps/web` routes / panels: Copilot **Briefs** tab; **Generate brief** on DES and Portfolio; `app/actions/briefs.ts`
- InsForge functions: `brief-service`; `market-tick` invokes cron
- Migrations: `insforge/migrations/0023_briefs.sql` (CLI twin `migrations/20260914140000_briefs.sql`)

## Contracts

- Zod schemas (`@meridian/schemas`): `briefSchema`, `briefKindSchema`, `briefGenerateRequestSchema`, `briefListResponseSchema`, `briefExportResponseSchema`, `briefCronResponseSchema`, `portfolioAnalysisFactsSchema`
- Endpoints / records: `POST /functions/brief-service` (`generate` / `list` / `export` JWT; `cron` service key); table `briefs`; storage bucket `briefs`
- Events / realtime: none new
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-RISK-02 (`portfolio_analysis`, COLLECT), DT-ENT-01
- Entitlements / `authorize()`: `copilot:chat` on generate/list/export/opt-in; cron is service-key; `audit_log` on `briefs:generate`, `briefs:export`, `briefs:cron`, `briefs:opt_in`

## How to extend

- New brief kind: add Zod enum + generator pack in `@meridian/copilot` + `brief-service` `buildPack`
- Keep Portfolio Health numbers from `evaluateDomain('portfolio_analysis')` / `assemblePortfolio` — do not fork P&L or copy DT thresholds into prompts
- Rebuild `pnpm functions:bundle:brief-service` (and `market-tick` if cron wiring changes)
- Do not index `docs/` or `docs/kb/` into news RAG

## Ops

- Env vars (names only): `API_KEY` / `INSFORGE_API_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `OPENROUTER_API_KEY`, `OPENROUTER_CHAT_MODEL`, `OPENROUTER_CHAT_URL`, `MERIDIAN_COPILOT_LLM`, `BRIEFS_BUCKET`, `E2E_AUTH_STUB`, `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`
- Seed / fixtures: published DT-RISK-02 via `pnpm seed:rules`; stub news ids in `stub-store`
- Test harness notes: Vitest `@meridian/copilot` `briefs.test.ts` `@TC-028-02`; Playwright `apps/web/e2e/briefs.spec.ts` `@TC-028-01` with `E2E_AUTH_STUB=1`. Apply **0023**, deploy `brief-service`, create storage bucket `briefs`.

## Trace

- Commit: `feat(PBI-028): AI research briefs and portfolio insights`
- SHA: this commit
