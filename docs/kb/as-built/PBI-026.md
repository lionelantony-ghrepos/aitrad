# PBI-026 — Copilot actions with approval workflow

## PBI / ACs / TCs

- PBI: [PBI-026](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-026-01, AC-026-02, AC-026-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-026-01, TC-026-02, TC-026-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Spec prompt said migration 0013; 0013 is screener, so `copilot_actions` is **0019**. **0020** makes JWT SELECT-only; INSERT/UPDATE are `project_admin`. Approve/reject goes through `copilot-orchestrator` (not user REST PATCH). Admin persist requires owned session + matching `user_id`. Admin watchlist add verifies `watchlists.user_id`. |

## Surfaces

- Packages: `@meridian/copilot` (`WRITE_TOOLS`, `handleWriteToolCall`, `decidePersistedAction`, `actionAwareLlm`, summaries); `@meridian/schemas` write-tool + `copilot_actions` DTOs
- `apps/web` routes / panels: Copilot approval cards; status-bar notification center pending list; `POST /api/copilot/chat` stub write path; `app/actions/copilot-actions.ts`
- InsForge functions: `copilot-orchestrator` write-tool registration + `copilot_actions` persist
- Migrations: `0019_copilot-actions.sql`; `0020_copilot-actions-jwt-select.sql`; CLI twins `20260914100000_copilot-actions.sql`, `20260914110000_copilot-actions-jwt-select.sql`

## Contracts

- Zod schemas (`@meridian/schemas`): `copilotWriteToolNameSchema`, `proposeOrderToolInputSchema`, `createWatchlistItemToolInputSchema`, `createAlertToolInputSchema`, `createMonitorToolInputSchema`, `copilotActionSchema`, `copilotActionDecideRequestSchema`, `writeToolResultSchema`; SSE `action` event
- Endpoints / records: `POST /functions/copilot-orchestrator` (chat SSE or decide JSON); `decideCopilotActionAction` / `listCopilotActionsAction`; table `copilot_actions` (JWT SELECT); RPC `count_copilot_user_actions_today`
- Events / realtime: existing SSE plus `action`; blotter refresh via `notifyOrdersChanged` after order approve
- Generated reference: [generated/](../generated/)

## Rules

- Decision tables evaluated or seeded: DT-AI-01, DT-ENT-01; approved orders still evaluate DT-VAL-01 / DT-RISK-01 / DT-HRS-01 / DT-FEE-01 via order-service
- Entitlements / `authorize()`: `copilot:chat` on chat; `copilot:act` on list/decide; execute uses `trade:create` / `watchlist:item:create` / `alerts:create`; tool calls write `audit_log` (`copilot:tool:*`, `copilot:action:approve|reject`)

## How to extend

- PBI-027 monitors: persist compiled conditions into `monitors`; keep `create_monitor` on this policy path
- New write tool: Zod input in `@meridian/schemas`, row in `WRITE_TOOLS`, executor in `runManualWrite` + orchestrator `executeManualWriteOnEdge`
- Do not auto-execute `propose_order` in application code — change DT-AI-01 instead
- Rebuild `pnpm functions:bundle:copilot-orchestrator` after source changes

## Ops

- Env vars (names only): same as PBI-025 (`OPENROUTER_API_KEY`, `MERIDIAN_COPILOT_LLM`, `E2E_AUTH_STUB`, InsForge URL/key names)
- Seed / fixtures: published DT-AI-01 via `pnpm seed:rules`; stub quotes/instruments
- Test harness notes: Vitest `@meridian/copilot` `write-actions.test.ts` `@TC-026-01` `@TC-026-02` `@TC-026-03`; Playwright `apps/web/e2e/copilot-actions.spec.ts`; apply 0019 then deploy `copilot-orchestrator`

## Trace

- Commit: `feat(PBI-026): copilot write tools and approval workflow`
- SHA: this commit
