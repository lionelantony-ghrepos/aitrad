# PBI-027 — Monitors (autonomous standing agents)

## PBI / ACs / TCs

- PBI: [PBI-027](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-027-01, AC-027-02, AC-027-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-027-01, TC-027-02, TC-027-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Spec prompt said migration 0014; 0014 is alerts, so `monitors` is **0021**. `alerts.alert_rule_id` is nullable with XOR `monitor_id`. Compiler tries deterministic canned NL first, then LLM JSON + Zod with one retry. Runner is service-key `monitor-runner` (eval/fire is not JWT). JWT owner CRUD is 0021 RLS; **0022** limits JWT UPDATE to `name`/`active`/`throttle_state` (pause + REST throttle reset). News sentiment uses `search_news_hybrid` plus `news_items` fallback. Feed test-mode is `force_position_day_pct` on the runner (and `/api/e2e/run-monitors` in `E2E_AUTH_STUB`). Hosts still on PBI-026 (`pbi026-local` / `44ef4d3`) must apply 0021+0022 and deploy `monitor-runner` + `copilot-orchestrator`. |

## Surfaces

- Packages: `@meridian/copilot` (`compileMonitorInstruction`, golden cases, explainers); `@meridian/rules-engine` (`runMonitorCycle`); `@meridian/schemas` monitor DTOs
- `apps/web` routes / panels: Copilot **Monitors** tab (pause/resume, reset throttle, explain, history); `app/actions/monitors.ts`; stub evaluate `/api/e2e/run-monitors`
- InsForge functions: `monitor-runner`; `create_monitor` persist in `copilot-orchestrator`; `market-tick` / `news-ticker` invoke runner
- Migrations: `insforge/migrations/0021_monitors.sql` (CLI twin `migrations/20260914120000_monitors.sql`); `0022_monitors-jwt-owner-patch.sql` (CLI twin `migrations/20260914130000_monitors-jwt-owner-patch.sql`)

## Contracts

- Zod schemas (`@meridian/schemas`): `monitorSchema`, `monitorScopeSchema`, `compiledMonitorConditionSchema`, `monitorCompileResultSchema`, `monitorRunnerRequestSchema`, `monitorCreateRequestSchema`; `alertInstanceSchema.monitor_id` / nullable `alert_rule_id`
- Endpoints / records: owner CRUD `monitors` (JWT); JWT PATCH `throttle_state: {}` to reset; `POST /functions/monitor-runner` (service bearer only); RPC `count_user_monitors`; copilot `create_monitor` writes compiled rows + `audit_log` `monitors:create`
- Events / realtime: existing `alerts:{userId}` `alert` events
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-ALRT-01, DT-AI-01, DT-ENT-01
- Entitlements / `authorize()`: `copilot:act` create/pause/delete/throttle-reset; `copilot:chat` list; runner is service-key; `audit_log` on create/pause/delete/`monitors:throttle_reset`/`monitor:fire`

## How to extend

- New canned NL: add a matcher in `compileMonitorInstruction` and a frozen row in `MONITOR_GOLDEN_EXPECTED`
- Keep throttle via `evaluateDomain('alerting')` — do not fork DT-ALRT math
- Rebuild `pnpm functions:bundle:monitor-runner` and `pnpm functions:bundle:copilot-orchestrator` after source changes
- Do not index `docs/` or `docs/kb/` into news RAG

## Ops

- Env vars (names only): `API_KEY` / `INSFORGE_API_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `OPENROUTER_API_KEY`, `OPENROUTER_CHAT_MODEL`, `OPENROUTER_CHAT_URL`, `MERIDIAN_COPILOT_LLM`, `E2E_AUTH_STUB`, `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`
- Seed / fixtures: published DT-ALRT-01 / DT-AI-01 via `pnpm seed:rules`; golden NL in `@meridian/copilot`
- Test harness notes: Vitest `@meridian/copilot` `monitor-compiler.test.ts` `@TC-027-01`; `@meridian/rules-engine` `monitor-cycle.test.ts` `@TC-027-02` `@TC-027-03`; `apps/web` `evaluate-stub.test.ts`; Playwright `apps/web/e2e/monitors.spec.ts`. Apply **0021 then 0022**, then deploy `monitor-runner` and rebuilt `copilot-orchestrator`. A harness left on `44ef4d3` has no `monitors` table.

## Trace

- Commit: `feat(PBI-027): monitors, compiler, and monitor-runner`
- SHA: this commit
