# PBI-025 — Copilot chat & read tools

## PBI / ACs / TCs

- PBI: [PBI-025](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-025-01, AC-025-02, AC-025-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-025-01, TC-025-02, TC-025-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Spec prompt said migration 0012; 0012 is fundamentals, so sessions/messages are **0017**. Streaming is SSE (`text/event-stream`) from `copilot-orchestrator` and `/api/copilot/chat`. Live LLM is OpenRouter via the Model Gateway key (`OPENROUTER_API_KEY`); `MERIDIAN_COPILOT_LLM=fake` and `E2E_AUTH_STUB` use the scripted news-summary transcript. Write tools stay PBI-026. |

## Surfaces

- Packages: `@meridian/copilot` (loop, fake LLM, citations, DT-AI-01 rate-limit adapter); `@meridian/schemas` (`src/copilot.ts`)
- `apps/web` routes / panels: Copilot panel; `POST /api/copilot/chat`; `app/actions/copilot.ts`; palette `AI <q>` already routed (PBI-009)
- InsForge functions: `copilot-orchestrator` (`copilot-orchestrator-src.ts`)
- Migrations: `insforge/migrations/0017_copilot-sessions.sql`; CLI twin `migrations/20260911210000_copilot-sessions.sql`

## Contracts

- Zod schemas (`@meridian/schemas`): `copilotChatRequestSchema`, `copilotChatEventSchema`, `copilotSessionSchema`, `copilotMessageSchema`, read-tool input schemas
- Endpoints / records: `POST /functions/copilot-orchestrator` (user JWT + SSE); `POST /api/copilot/chat` (session cookie); tables `copilot_sessions`, `copilot_messages`; RPC `count_copilot_user_messages_today`
- Events / realtime: SSE `session` / `tool_start` / `tool_end` / `token` / `message` / `rate_limited` / `error`
- Generated reference: [generated/](../generated/)

## Rules

- Decision tables evaluated or seeded: DT-AI-01 (`ai_action_policy`, `messages_today` → `rate_limit`), DT-ENT-01 (`copilot:chat`)
- Entitlements / `authorize()`: `copilot:chat` on chat + session list/load; every tool call writes `audit_log` (`copilot:tool:<name>`)

## How to extend

- PBI-026 write tools: register in `@meridian/copilot` + evaluate `ai_action_policy` per tool; do not auto-execute orders
- New read tool: Zod input in `@meridian/schemas`, row in `READ_TOOLS`, executor in orchestrator + stub `executeStubReadTool`
- `search_news` must keep calling `search-news` / hybrid RAG — do not ingest `docs/` or `docs/kb/`
- Rebuild `pnpm functions:bundle:copilot-orchestrator` after source changes

## Ops

- Env vars (names only): `OPENROUTER_API_KEY`, `OPENROUTER_CHAT_MODEL`, `OPENROUTER_CHAT_URL`, `MERIDIAN_COPILOT_LLM`, `API_KEY` / `INSFORGE_API_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `E2E_AUTH_STUB`
- Seed / fixtures: stub AAPL news ids in `stub-store`; published DT-AI-01 via `pnpm seed:rules`
- Test harness notes: Vitest `@meridian/copilot` `loop.test.ts` `@TC-025-02` `@TC-025-03`; Playwright `apps/web/e2e/copilot.spec.ts` `@TC-025-01` with `E2E_AUTH_STUB=1`. Apply 0017 then deploy `copilot-orchestrator`.

## Trace

- Commit: `feat(PBI-025): copilot chat, read tools, and session persistence`
- SHA: this commit
