# PBI-015 — Paper matching engine

## PBI / ACs / TCs

- PBI: [PBI-015](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-015-01, AC-015-02, AC-015-03, AC-015-04, AC-015-05 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-015-01, TC-015-02, TC-015-03, TC-015-04

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | `match()` is the fills-only entry; `matchOrders()` also returns newly triggered stop ids so stop-limit can persist `stop_triggered` without a fill. Liquidity is a per-order share cap derived from ADV × `liquidity_cap_pct_adv` (not a shared book). Large-notional adder rows in the seeded table still key on `large_notional`; the runner passes `order_notional` + `avg_volume_band` and does not invent that boolean. `accepted → working` happens on the first matching-runner pass for the ticked instrument. `apply_paper_fill` asserts `p_filled_qty = ord.filled_qty + p_qty` and that `p_cash_delta` equals ±qty×price (buy debit / sell credit), then `SELECT … FROM positions … FOR UPDATE` before upsert. Doc 04 AC/TC-015 boxes stay ☐ pending independent RV/QA. TC-015-03 is an in-process cycle against feed-style ticks (limit rests, then a forced last crosses). No blotter UI (PBI-017). |

## Surfaces

- Packages: `@meridian/paper-engine` (`match.ts`, `exec-config.ts`, `positions.ts`, `settle.ts`, `cycle.ts`); `@meridian/schemas` (exec/match/runner DTOs; `orders.stop_triggered`)
- `apps/web` routes / panels: none (fills are server-side; ticket already shipped)
- InsForge functions: `matching-runner` (`insforge/functions/matching-runner-src.ts`, bundle `matching-runner.ts`); `market-tick` now POSTs its published ticks to matching-runner
- Migrations: `insforge/migrations/0009_paper-matching.sql`; CLI twin `migrations/20260909120000_paper-matching.sql`

## Contracts

- Zod schemas (`@meridian/schemas`): `execConfigSchema`, `matchTickSchema`, `workingOrderMatchSchema`, `matchFillSchema`, `matchingRunnerRequestSchema`, `matchingRunnerResponseSchema`; `orderRecordSchema.stop_triggered`
- Endpoints / records: `POST /functions/matching-runner` (service key); `apply_paper_fill` RPC; `orders.stop_triggered`; `executions` / `positions` / `accounts` cash + reserve
- Events / realtime: `orders:{userId}` event `order`; `positions:{userId}` event `position` via `publish_position_event`
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-EXEC-01 (`evaluateDomain('execution_sim')`)
- Entitlements / `authorize()`: service bearer `API_KEY` / `INSFORGE_API_KEY` (not a user mutating API; DT-ENT-01 n/a). Per-fill `audit_log` (`trade:fill`, `trade:promote`)

## How to extend

- PBI-016 group/trailing logic should stay in `@meridian/paper-engine` and be invoked from matching-runner; do not fork a second matcher
- Call `assertTransition` / `canTransition` and `apply_paper_fill` via admin + `p_user_id` — do not grant authenticated EXECUTE
- Rebuild `pnpm functions:bundle:matching-runner` and `pnpm functions:bundle:market-tick` after engine or tick-hook changes
- Change slippage / liquidity in DT-EXEC-01, not in `match()`

## Ops

- Env vars (names only): `API_KEY`, `INSFORGE_API_KEY`, `INSFORGE_INTERNAL_URL`, `INSFORGE_BASE_URL`
- Seed / fixtures: published DT-EXEC-01 via `pnpm seed:rules`; apply 0009 via `npx -y @insforge/cli db migrations up 20260909120000_paper-matching.sql`
- Test harness notes: Vitest `@meridian/paper-engine` `match.test.ts` (TC-015-01), `positions.test.ts` (TC-015-02), `cycle.test.ts` (TC-015-03/04); `apps/web` `migration-0009.test.ts`. Live: pause feed, place limit below last, `feed.force_price` through the limit, invoke `market-tick` then matching-runner (or rely on the tick hook)

## Trace

- Commit: `feat(PBI-015): paper matching engine and matching-runner`
- SHA: this commit
