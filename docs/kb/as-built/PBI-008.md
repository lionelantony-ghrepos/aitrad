# PBI-008 — Chart panel

## PBI / ACs / TCs

- PBI: [PBI-008](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-008-01, AC-008-02, AC-008-03, AC-008-04 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-008-01, TC-008-02, TC-008-03, TC-008-04

## Shipped vs spec

| Item | Status |
| --- | --- |
| Lightweight Charts candlesticks + volume histogram | done |
| Range switcher 1D/1W/1M/1Y/5Y (1D → 1m; 1W–5Y → 1d) | done |
| Ticks from `useQuotes` applied to the current candle | done |
| SMA 20/50/200, EMA 12/26, VWAP overlays; RSI(14) sub-pane | done |
| Crosshair / last-bar legend with OHLCV + enabled indicators | done |
| Empty / idle / error / loading panel states | done |
| Stale fetch ignore + AbortController generation gate | done |
| E2E GIP MSFT | deferred — function routing is PBI-009; TC-008-01 uses watchlist click on MSFT then cycles ranges |
| Live InsForge `market_bars` in CI Playwright | stub bars when `E2E_AUTH_STUB` |

## Surfaces

- Packages: `@meridian/indicators` (SMA/EMA/VWAP/RSI); `@meridian/schemas` (`chartRangeSchema`, `chartBarsQuerySchema`, `chartBarsResponseSchema`)
- `apps/web` routes / panels: `/workspace` Chart panel (`chart`); `GET /api/chart/bars`
- InsForge functions: none
- Migrations: none

## Contracts

- Zod (`@meridian/schemas`): `chartRangeSchema`, `chartBarsQuerySchema`, `chartBarsResponseSchema`; bars remain `marketBarSchema`
- Endpoints / records: `GET /api/chart/bars?symbol=&range=`; reads `instruments`, `market_bars`
- Events / realtime: channel `quotes`, event `tick_batch` (same as PBI-006/007). Test feed: `meridian:tick_batch`
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: none (display lookbacks and indicator periods are UI math, not DT cells)
- Entitlements / `authorize()`: `chart:bars` authenticated-only; DT-ENT-01 still PBI-024. GET is non-mutating (no `audit_log`)

## How to extend

- PBI-009 `GIP <sym>` should focus the Chart panel and `setActiveSymbol`
- Keep indicator formulas in `@meridian/indicators`; do not reimplement in the panel
- Register further overlays in `lib/chart/overlays.ts` and the Chart toolbar
- Continue using `useSymbolContext` + `useQuotes`; do not add a second tick generator

## Ops

- Env vars (names only): `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `E2E_AUTH_STUB`
- Seed / fixtures: live `market_bars` from PBI-005 seed; stub AAPL/MSFT series when `E2E_AUTH_STUB`
- Test harness notes: Vitest `@meridian/indicators` TC-008-02; web range/tick/stale tests; Playwright `apps/web/e2e/chart.spec.ts` `@TC-008-01` / `@TC-008-03` / `@TC-008-04` with `E2E_AUTH_STUB=1`

## Trace

- Commit: `feat(PBI-008): chart panel with live candles and indicators`
- SHA: this commit
