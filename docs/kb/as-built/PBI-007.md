# PBI-007 — Realtime quote client & Watchlist panel

## PBI / ACs / TCs

- PBI: [PBI-007](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-007-01, AC-007-02, AC-007-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-007-01, TC-007-02, TC-007-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| `useQuotes(symbols[])` + coalesce ≤4 renders/sec | done |
| Zustand `symbolContext` (`activeSymbol`, setter) | done |
| Watchlist panel: named tabs, typeahead, live columns, sparkline, flash, sort, row click, remove | done |
| Persist selected list with workspace layout (`selectedWatchlistId`) | done |
| Unique `(watchlist_id, instrument_id)` + friendly duplicate message | done |
| Migration number | spec said `0003`; that id is market calendar/feed. Watchlists shipped as `0004` |
| Live InsForge realtime in CI Playwright | stub/test-mode window `tick_batch` events (`E2E_AUTH_STUB`) |

## Surfaces

- Packages: `@meridian/schemas` (watchlist DTOs; layout `selectedWatchlistId`)
- `apps/web` routes / panels: `/workspace` Watchlist panel; `symbol-context-readout` for consumers/tests
- InsForge functions: none (records + existing `quotes` channel)
- Migrations: `insforge/migrations/0004_watchlists.sql`; CLI twin `migrations/20260904120000_watchlists.sql`

## Contracts

- Zod (`@meridian/schemas`): `watchlistSchema`, `watchlistInsertSchema`, `watchlistPatchSchema`, `watchlistItemSchema`, `watchlistItemInsertSchema`, `duplicateWatchlistItemErrorSchema`; `workspaceLayoutV1Schema.selectedWatchlistId`
- Records: `watchlists`, `watchlist_items`; reads `instruments`, `quotes_latest`
- Events / realtime: channel `quotes`, event `tick_batch` (same as PBI-006). Test feed: `meridian:tick_batch` CustomEvent
- Server actions: `app/actions/watchlists.ts` (CRUD + search + quote hydrate)
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: none (display math only; no policy cells)
- Entitlements / `authorize()`: mutating watchlist actions gated as authenticated-only (`watchlist:*`); DT-ENT-01 still PBI-024. `audit_log` on live creates/updates/deletes

## How to extend

- PBI-008 Chart should `useSymbolContext` + `useQuotes` for the active symbol; do not add a second tick generator
- Register further panels in `panel-registry.ts`; keep Watchlist `component` id `watchlist`
- Next rules migration is `0005` (PBI-011 prompt still says 0004 — use the next free number)
- Do not copy DT-* thresholds into the panel; sort/flash/sparkline window sizes are UI, not policy

## Ops

- Env vars (names only): `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `E2E_AUTH_STUB`
- Seed / fixtures: stub universe AAPL/MSFT when `E2E_AUTH_STUB`; live typeahead reads `instruments`
- Test harness notes: Vitest coalesce/sort/duplicate/layout; Playwright `apps/web/e2e/watchlist.spec.ts` `@TC-007-01` / `@TC-007-02` / `@TC-007-03` with `E2E_AUTH_STUB=1`. Apply 0004 via `npx -y @insforge/cli db migrations up --all`

## Trace

- Commit: `feat(PBI-007): watchlist panel, coalesced quotes, symbolContext`
- SHA: this commit
