# PBI-020 — Fundamentals & DES (instrument profile)

## PBI / ACs / TCs

- PBI: [PBI-020](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-020-01, AC-020-02 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-020-01, TC-020-02

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | PRD named this migration 0007; 0007 already creates `orders`, so `fundamentals` is **0012**. Seed file `mock_data/fundamentals.json` stays the doc 06 flat shape; `@meridian/mock-data` hydrates the nested jsonb groups (valuation, income, margins, dividends, ranges, analyst) plus 4-period revenue/EPS series. `DES` was already registered in PBI-009; this PBI replaces the Description placeholder. ETF rows omit equity fields; DES shows em dash rather than crashing (AC-020-01). |

## Surfaces

- Packages: `@meridian/mock-data` (`src/fundamentals.ts`); `@meridian/schemas` (`src/fundamentals.ts`)
- `apps/web` routes / panels: workspace `des` panel; `app/actions/des.ts`; `lib/des/view.ts`
- InsForge functions: none
- Migrations: `insforge/migrations/0012_fundamentals.sql`; CLI twin `migrations/20260909190000_fundamentals.sql`

## Contracts

- Zod schemas (`@meridian/schemas`): `analystRatingsSchema`, `fourPeriodSeriesSchema`, `fundamentalsMetricsSchema`, `fundamentalsRecordSchema`, `fundamentalsFileRowSchema`, `desProfileSchema`, `desPeerSchema`
- Endpoints / records tables: SELECT `fundamentals`; writes via `pnpm seed` (`project_admin`)
- Events / realtime: none (last px on the 52w marker may follow existing `quotes` ticks)
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: none (mock sector ranges live only in the seed generator)
- Entitlements / `authorize()`: n/a (read-only). `getDesProfileAction` requires a session JWT (`getSessionUser` + `getAccessToken`) and uses that token on the records client, not a service role. Migration 0012 grants SELECT to `anon`/`authenticated` and SELECT/INSERT/UPDATE/DELETE to `project_admin` (no INSERT policy for those roles). Browser/client code does not use a service-role key. There is no DES edge function; writes are seed/`project_admin` only.

## Residual (non-blocking)

- RLS `fundamentals_select_public` still allows `anon` SELECT (`USING (true)`). The app JWT-gates reads in `getDesProfileAction`.

## How to extend

- PBI-021 screener should `JOIN fundamentals` via parameterized SQL; reuse `fundamentalsMetricsSchema` paths (`valuation.pe`, `dividends.dividend_yield`, `ranges.week52_*`) — do not fork a second metrics shape
- Copilot `get_fundamentals` (later) should call the same repository / Zod parse
- New metric fields: add to the nested Zod group, hydrate in `generateFundamentalsMetrics`, then 001N only if SQL CHECKs change
- Do not rewrite `mock_data/fundamentals.json` into a second universe; hydrate at seed

## Ops

- Env vars (names only): `INSFORGE_URL`, `INSFORGE_API_KEY`, `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `E2E_AUTH_STUB`
- Seed / fixtures: `mock_data/fundamentals.json`; `pnpm seed` upserts one row per instrument
- Test harness notes: Vitest `@meridian/mock-data` `fundamentals.test.ts` (TC-020-01 property + generator); `apps/web/lib/des/view.test.ts`; Playwright `apps/web/e2e/des.spec.ts` `@TC-020-01` `@TC-020-02` with `E2E_AUTH_STUB=1`. Source-lock: `getDesProfileAction` JWT + user token for records, 0012 SELECT-only, no DES edge write path. Doc 04 AC/TC-020 ticks are QA-owned (left ☐). Apply with `npx -y @insforge/cli db migrations up --all`

## Trace

- Commit: `feat(PBI-020): fundamentals table and DES instrument profile`
- SHA: this commit
