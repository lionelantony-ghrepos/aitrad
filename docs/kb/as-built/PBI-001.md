# PBI-001 — Monorepo scaffold & CI

## PBI / ACs / TCs

- PBI: [PBI-001](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-001-01, AC-001-02 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-001-01, TC-001-02
- Doc 04 Status boxes for these IDs remain ☐ (not ticked in-repo)

## Shipped vs spec

| Item | Status |
| --- | --- |
| pnpm workspaces + Turborepo; `apps/web` Next.js 15 App Router, strict TS, Tailwind, shadcn/ui | done |
| Empty packages with tsconfig, Vitest, placeholder tests | done (`rules-engine`, `paper-engine`, `mock-data` still placeholders) |
| ESLint (typescript-eslint), Prettier, Husky + lint-staged | done |
| GitHub Actions: typecheck, lint, test on PR/push | done (pnpm pinned **9.15.9** in CI) |
| Workspace / dockable panels | deferred to PBI-003 (`app/page.tsx` is a landing placeholder) |
| Root README “application code is not scaffolded” | spec gap vs the tree — do not treat README Status as source of truth |

## Surfaces

- Packages: `@meridian/schemas`, `@meridian/rules-engine`, `@meridian/paper-engine`, `@meridian/mock-data` (`pnpm-workspace.yaml`: `apps/*`, `packages/*`)
- `apps/web` routes / panels: `/` only (`app/page.tsx`, `app/layout.tsx`); `components/ui/button.tsx`; no dockview/panels
- InsForge functions: none
- Migrations: none (PBI-002)
- Tooling: `turbo.json` pipelines `build` / `test` / `lint` / `typecheck`; `.github/workflows/ci.yml` (later also `docs:kb-check` / `docs:generate`)

## Contracts

- Zod (`@meridian/schemas`): `publicInsforgeEnvSchema` (browser InsForge URL + anon key). Entity DTOs are PBI-002.
- Endpoints / records tables: none
- Events / realtime: none
- Generated reference: [generated/packages.md](../generated/packages.md)

## Rules

- Decision tables evaluated or seeded: none
- Entitlements / `authorize()`: n/a

## How to extend

- New workspace package: add under `packages/` or `apps/`, include Turbo `typecheck` / `test` / `lint` scripts
- Shared DTOs: `@meridian/schemas` first, then import from app/packages
- Do not add a second package manager or a second Next app for v1
- Policy math stays out of this layer — `@meridian/rules-engine` is a placeholder until PBI-010 (`engineReady()`)
- Paper matching stays in `@meridian/paper-engine` until PBI-015 (`matcherReady()`)
- Seed generators stay in `@meridian/mock-data` until PBI-005; static JSON remains in `/mock_data`

## Ops

- Env vars (names only): `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY` (`apps/web/.env.example`)
- Seed / fixtures: none in this PBI
- Test harness: `pnpm test` via Turbo/Vitest; Node **≥20.11**; packageManager **pnpm@9.15.9** (`.nvmrc`, CI `setup-node` 20)
- Lint-staged runs ESLint + Prettier on commit

## Trace

- Commits: `03dfc69` PBI-001 scaffold; `a4ac5ce` fix(PBI-001) pin CI pnpm; `2f7d350` PBI-001 merge (#66)
- SHAs: `03dfc697e0f654e1f458178ec2ca7b687338ddad`, `a4ac5ce`, `2f7d3501fecdb4816ae016cb580d78b6c8643415`
