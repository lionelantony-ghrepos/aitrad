# PBI-030 — Observability, resilience & performance pass

## PBI / ACs / TCs

- PBI: [PBI-030](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-030-01, AC-030-02 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-030-01, TC-030-02

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | `/telemetry` is the Next.js browser path; edge `telemetry` is the InsForge ingest. Function latency samples also insert directly from `_shared/logger.ts` (no extra hop). Architecture §8 TTI/p95/rps and the PBI feed-gap watermark live in `@meridian/schemas` observability constants (engineering SLOs, not DT cells). Lighthouse CI collects `/workspace` (auth redirect on CI profile is still the workspace route) plus Playwright TTI on a signed-in shell. k6 hits live `/preview`; Vitest asserts the script thresholds. Republish DT-ENT-01 so trader `telemetry:write` is in the published table. |

## Surfaces

- Packages: `@meridian/schemas` (`observability.ts`, `telemetry.ts`); `@meridian/rules-engine` (`observability.ts`, `telemetry.ts` handlers)
- `apps/web` routes / panels: `POST /telemetry`; `/admin/health`; panel `ErrorBoundary` + STALE watermark; status-bar realtime states
- InsForge functions: `telemetry`, `health-service`; `withFunctionLog` on every `*-src` default export + `provision-account`; `market-tick` heartbeat
- Migrations: `insforge/migrations/0027_telemetry.sql` (CLI twin `migrations/20260914180000_telemetry.sql`)

## Contracts

- Zod schemas (`@meridian/schemas`): `functionLogSchema`, `telemetryRequestSchema`, `telemetryRecordSchema`, `healthSnapshotSchema`
- Endpoints: `POST /telemetry` `{ op: "client_error" \| "realtime" }`; `POST /functions/telemetry` also `fn_latency` (service); `POST /functions/health-service` `{ op: "snapshot" }`
- Records: `telemetry` (JWT no DML); `feature_flags` `feed.last_heartbeat`
- Events / realtime: quotes transport reconnect/backoff; client `realtime` telemetry samples
- Generated reference: [generated/](../generated/)

## Rules

- Decision tables evaluated or seeded: DT-ENT-01
- Entitlements / `authorize()`: `telemetry:write` (trader + admin via baseline); `health:read` (admin). Mutating ingest writes `audit_log`.

## How to extend

- New edge function: wrap `export default withFunctionLog("slug", handler)` and rebundle.
- New client panel: `wrapPanel(id, Component)` so crashes stay isolated.
- Do not JWT REST-insert `telemetry`. Do not copy Architecture §8 numbers into unrelated UI branches.
- Rebuild `pnpm functions:bundle:telemetry` and `pnpm functions:bundle:health-service`. Apply **0027**. Republish DT-ENT-01 after pulling baseline `telemetry:write`.

## Ops

- Env vars (names only): `TELEMETRY_SAMPLE_RATE`, `API_KEY` / `INSFORGE_API_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `E2E_AUTH_STUB`
- Seed / fixtures: `feed.last_heartbeat` row in 0027; k6 `scripts/load/order-preview.k6.js`; `lighthouserc.cjs`
- Test harness notes: Vitest `@TC-030-01` panel crash hook + logger wrap + handler tests; `@TC-030-02` budget files + Playwright TTI; Playwright `e2e/observability.spec.ts`. k6 against dev: `INSFORGE_URL`, `ACCESS_TOKEN`.

## Trace

- Commit: `feat(PBI-030): observability, reconnect, and perf budgets`
- SHA: this commit
