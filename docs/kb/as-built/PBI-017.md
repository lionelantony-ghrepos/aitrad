# PBI-017 — Blotter (orders & executions)

## PBI / ACs / TCs

- PBI: [PBI-017](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-017-01, AC-017-02, AC-017-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-017-01, TC-017-02, TC-017-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Tabs are Working / Filled / Rejected / All (Working includes `accepted`, `working`, `partially_filled`). Modify is cancel/replace: blotter calls existing order-service cancel, then prefills the ticket; there is no dedicated replace endpoint. Live updates subscribe to `orders:{userId}` event `order` and refetch; stub/E2E uses `meridian:orders-changed` plus the existing tick-batch matching hook. Default tab is All so group legs stay visible (PBI-016). No new migration. |

## Surfaces

- Packages: `@meridian/schemas` (`orderRealtimeEventSchema`, `blotterTabSchema`, `blotterFiltersSchema`)
- `apps/web` routes / panels: workspace `blotter` panel; ticket prefill via `useOrderTicketIntent.applyPrefill`
- InsForge functions: none new (reuse `order-service` cancel/create)
- Migrations: none

## Contracts

- Zod schemas (`@meridian/schemas`): `orderRealtimeEventSchema`, `blotterTabSchema`, `blotterSideFilterSchema`, `blotterFiltersSchema`; existing `orderRecordSchema`, `executionRecordSchema`, `ruleAuditViewSchema`
- Endpoints / records: `POST /functions/order-service/orders/{id}/cancel`; `GET` `orders`, `executions`, `rule_audit` via records repositories
- Events / realtime: channel `orders:{userId}`, event `order`; E2E `meridian:orders-changed`
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: none new (Explain renders stored `rule_audit` from prior DT-VAL-01 / DT-RISK-01 / DT-HRS-01 / DT-FEE-01 evaluations)
- Entitlements / `authorize()`: cancel/modify reuse `trade:cancel` + `audit_log` on order-service; list/explain/executions are reads

## How to extend

- Keep blotter filter/tree/CSV in `apps/web/lib/blotter` — do not fork a second list (PBI-016 group-status list is this panel)
- New columns: add to TanStack defs and `blotterCsvRows` together
- Do not add a replace opcode; cancel then create remains the cancel/replace path
- Subscribe only to `orders:{userId}`; do not invent a second blotter channel

## Ops

- Env vars (names only): `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `API_KEY` / `INSFORGE_API_KEY`, `E2E_AUTH_STUB`
- Seed / fixtures: existing stub universe + published decision tables via `pnpm seed:rules`
- Test harness notes: Vitest `apps/web/lib/blotter/view.test.ts` (TC-017-03), `explain.test.ts` (TC-017-02), `orders-live.test.ts`; Playwright `apps/web/e2e/blotter.spec.ts` `@TC-017-01` / `@TC-017-02` / `@TC-017-03` with `E2E_AUTH_STUB=1`

## Trace

- Commit: `feat(PBI-017): blotter orders and executions panel`
- SHA: this commit
