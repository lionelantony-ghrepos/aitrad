# PBI-005 — Instrument master and mock universe seed

## PBI / ACs / TCs

- PBI: [PBI-005](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-005-01, AC-005-02, AC-005-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-005-01, TC-005-02

## Shipped vs spec

| Item | Status |
| --- | --- |
| `@meridian/mock-data` GBM history (seeded mulberry32, NYSE sessions, 1d + 1m) | done |
| `scripts/seed.ts` idempotent batched upsert + SQL count report | done |
| Migration 0002 `market_bars` / `quotes_latest` | done |
| Static universe path | spec says `mock-data/`; this repo uses `mock_data/instruments.json` (same file, not a second universe) |
| Full `scripts/seed-all.ts` pipeline (calendar, rules, news, demo users) | deferred to later PBIs |
| Live SQL counts in CI | deferred; Vitest covers generator + `evaluateSeedCounts`; `pnpm seed` prints the SQL check against a linked project |

## Surfaces

- Packages: `@meridian/mock-data` (calendar, DT-SIM-01 sim params, GBM, count gate); `@meridian/schemas` (mock instrument / bar / quote DTOs)
- `apps/web` routes / panels: none (read repositories only: `market-bars`, `quotes-latest`)
- InsForge functions: none
- Migrations: `insforge/migrations/0002_market-bars-and-quotes.sql`; CLI twin `migrations/20260902124300_market-bars-and-quotes.sql`

## Contracts

- Zod (`@meridian/schemas`): `mockInstrumentSchema`, `instrumentUpsertSchema`, `marketBarSchema`, `quotesLatestSchema`, `barTimeframeSchema`, `betaClassSchema`, `marketCapBandSchema`, `avgVolumeBandSchema`, `seedEnvSchema`; `instrumentSchema` optional generator columns
- Endpoints / records tables: `instruments` (extended), `market_bars`, `quotes_latest` via `recordTables`
- Events / realtime: none (PBI-006)
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-SIM-01 (local ALL-merge in `@meridian/mock-data` until PBI-010 publishes tables)
- Entitlements / `authorize()`: n/a (ops seed, not a user mutating endpoint)

## How to extend

- PBI-006 should import `generateInstrumentHistory` / `simParamsForBeta` / `annualSigma` / `runFeedInvocation` rather than a second GBM
- New universe fields: add to `mock_data/instruments.json` + `mockInstrumentSchema` + 000N ALTER, then re-run `pnpm seed`
- Do not fork a second instruments JSON tree
- Do not put vol/gap cells in UI or kb; change DT-SIM-01 (and later rules-service) instead

## Ops

- Env vars (names only): `INSFORGE_URL`, `INSFORGE_API_KEY`
- Seed / fixtures: `mock_data/instruments.json`; `pnpm seed` (`tsx scripts/seed.ts`)
- Test harness notes: `packages/mock-data` Vitest (AC-005-01/02/03, TC-005-02, count gate); `apps/web/lib/api/migration-0002.test.ts` for 0002 SQL; apply with `npx -y @insforge/cli db migrations up --all`

## Trace

- Commit: `feat(PBI-005): seed mock instrument universe and OHLCV history`
- SHA: this commit
