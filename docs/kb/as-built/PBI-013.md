# PBI-013 — Order ticket panel

## PBI / ACs / TCs

- PBI: [PBI-013](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-013-01, AC-013-02, AC-013-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-013-01, TC-013-02, TC-013-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Ticket + `order-service` `/preview` and `/orders` shipped. Preview evaluates DT-VAL-01, DT-RISK-01, and DT-FEE-01 via `rules-service` (stub E2E uses in-process `evaluateDomain` + published memory tables). Rule facts, notional, and fees use `quotes_latest.last` (fail closed if missing); client `last_price` is not a fallback. `/orders` inserts an `accepted` row via `createAdminClient` when preview passes (`API_KEY` / `INSFORGE_API_KEY` required; no authenticated INSERT). Buying-power reserve, market-hours (DT-HRS-01), and full FSM transitions stay PBI-014. Positions are treated as flat (qty 0) until PBI-015/018. |

## Surfaces

- Packages: `@meridian/schemas` (`src/orders.ts`); `@meridian/paper-engine` (`qty-mode.ts`, `preview.ts` — facts, fee apply, assemble)
- `apps/web` routes / panels: workspace `orderTicket` panel; server actions `app/actions/orders.ts`
- InsForge functions: `order-service` (`insforge/functions/order-service-src.ts`, bundle `order-service.ts`)
- Migrations: `insforge/migrations/0007_orders.sql`; CLI twin `migrations/20260908160000_orders.sql`

## Contracts

- Zod schemas (`@meridian/schemas`): `orderDraftSchema`, `orderPreviewRequestSchema`, `orderPreviewResponseSchema`, `orderCreateRequestSchema`, `orderCreateResponseSchema`, `orderRecordSchema`
- Endpoints: `POST /functions/order-service/preview` (`op: "preview"`); `POST /functions/order-service/orders` (`op: "create"`)
- Records: `orders` (owner SELECT RLS; authenticated SELECT-only; writes via order-service admin)
- Events / realtime: none (PBI-014 publishes `orders:{userId}`)
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-VAL-01, DT-RISK-01, DT-FEE-01
- Entitlements / `authorize()`: `trade:preview`, `trade:create` (authenticated-only helper; DT-ENT-01 still PBI-024)
- Mutating: preview and create write `audit_log`; preview also writes `rule_audit` through `rules-service`

## How to extend

- PBI-014 should keep this preview assembler and add FSM, atomic buying-power reserve, and DT-HRS-01 — do not fork a second fact builder
- Register ticket fields only via `orderDraftSchema`; do not add policy literals in the panel
- Fee numbers come from DT-FEE-01 outputs (`applyFeeSchedule`); do not copy rates into UI
- Rebuild `pnpm functions:bundle:order-service` after preview/engine changes
- Do not grant authenticated INSERT/UPDATE/DELETE on `orders`; keep writes on the service admin path

## Ops

- Env vars (names only): `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `API_KEY` / `INSFORGE_API_KEY`, `E2E_AUTH_STUB`
- Seed / fixtures: paper cash from `paperAccountSeed` / account row; stub universe AAPL/MSFT/NVDA/TSLA when `E2E_AUTH_STUB`
- Test harness notes: Vitest `@meridian/paper-engine` (`qty-mode.test.ts` TC-013-03, `preview.test.ts`); `apps/web/lib/order-ticket/run-preview.test.ts` TC-013-01; Playwright `apps/web/e2e/order-ticket.spec.ts` `@TC-013-01` / `@TC-013-02` / `@TC-013-03` with `E2E_AUTH_STUB=1`. Apply 0007 via `npx -y @insforge/cli db migrations up 20260908160000_orders.sql`

## Trace

- Commit: `feat(PBI-013): order ticket panel and preview endpoint`
- SHA: this commit
