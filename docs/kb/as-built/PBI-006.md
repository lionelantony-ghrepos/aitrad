# PBI-006 — Mock market feed (tick engine)

## PBI / ACs / TCs

- PBI: [PBI-006](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-006-01, AC-006-02, AC-006-03 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-006-01, TC-006-02, TC-006-03

## Shipped vs spec

| Item | Status |
| --- | --- |
| `@meridian/mock-data` GBM stepper, 1m bar roll, coalesce ≤4 batches/sec | done |
| `market_calendar` + NYSE 2026 sessions incl. half-days | done |
| `insforge/functions/market-tick` scheduled invoke; N ticks = `feed.speed` × `MARKET_TICK_INTERVAL_SECONDS` | done |
| `quotes_latest` upsert, 1m `market_bars` upsert, realtime `quotes` via `publish_quotes_batch` | done |
| `feature_flags` `feed.paused` / `feed.speed`; `feed.force_price` consumed once | done |
| News-sentiment drift nudge (DT-SIM-01 shock row) | deferred to PBI-019 |
| Live 10s/30s websocket TCs in CI | deferred; Vitest covers pause, coalesce, force-price, OPEN/CLOSED |

## Surfaces

- Packages: `@meridian/mock-data` (`feed.ts`, calendar sessions, `calendar-sql.ts`); `@meridian/schemas` (calendar + tick batch DTOs)
- `apps/web` routes / panels: status bar OPEN/CLOSED now uses `@meridian/mock-data` `nyseSessionState` (same 2026 sessions as the table)
- InsForge functions: `market-tick` (`insforge/functions/market-tick-src.ts` source, `market-tick.ts` deploy bundle)
- Migrations: `insforge/migrations/0003_market-calendar-and-feed.sql`; CLI twin `migrations/20260902170300_market-calendar-and-feed.sql`

## Contracts

- Zod (`@meridian/schemas`): `marketCalendarRowSchema`, `sessionKindSchema`, `feedForcePriceSchema`, `feedControlsValueSchema`, `quoteTickSchema`, `quoteTickBatchSchema`
- Records: `market_calendar`, `quotes_latest`, `market_bars`, `feature_flags`, `audit_log` (`market-tick`)
- Events / realtime: channel `quotes`, event `tick_batch`, payload `{ ts, ticks[] }`
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-SIM-01 (local ALL-merge in `@meridian/mock-data` until PBI-010)
- Entitlements / `authorize()`: cron/admin bearer `API_KEY` / `INSFORGE_API_KEY` (not a user mutating API; DT-ENT-01 n/a)

## How to extend

- PBI-007 should subscribe to `quotes` and coalesce renders; do not add a second tick generator
- Change vol/gap behaviour in DT-SIM-01 (then `sim-params.ts`), not in the edge function
- Rebuild `market-tick.ts` from `market-tick-src.ts` after feed math changes
- Do not fork a second NYSE calendar; extend `nyseTradingSessions` / 000N seed

## Ops

- Env vars (names only): `API_KEY`, `INSFORGE_API_KEY`, `INSFORGE_BASE_URL`, `INSFORGE_INTERNAL_URL`, `MARKET_TICK_INTERVAL_SECONDS`
- Seed / fixtures: `pnpm seed` upserts `market_calendar`; flags seeded in 0003
- Test harness notes: Vitest `@meridian/mock-data` `feed.test.ts`; `apps/web` migration-0003 + market-session; schedule with interval syntax (platform minimum, e.g. `1 second`) POST `/functions/market-tick` with bearer `API_KEY`

## Trace

- Commit: `feat(PBI-006): mock market-tick feed, NYSE calendar, and quote batches`
- SHA: this commit
