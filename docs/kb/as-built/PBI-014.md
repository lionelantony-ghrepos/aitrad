# PBI-014 — Order service & validation pipeline

## PBI / ACs / TCs

- PBI: [PBI-014](../../03-PRD-PBIs-and-Cursor-Prompts.md)
- ACs: AC-014-01, AC-014-02, AC-014-03, AC-014-04 ([doc 04](../../04-Test-Plan.md))
- TCs: TC-014-01, TC-014-02, TC-014-03, TC-014-04

## Shipped vs spec

| Item | Status |
| --- | --- |
| Spec intent | done |
| Notes | Spec prompt said migration **0005**; 0005 is rules storage and 0007 already created `orders`. Pipeline DDL is **0008** (reserved cash, executions, positions, snapshots, `reserve_buying_power` row lock, `orders:*` publish). Preview writes **no** `audit_log` (only `rule_audit` via `rules-service`). Create always inserts a row, including rejects with `reject_reason` + `rule_audit_id`. Matching `accepted → working` remains PBI-015. |

## Surfaces

- Packages: `@meridian/paper-engine` (`fsm.ts`, `buying-power.ts`, `order-pipeline.ts`, preview hours fact); `@meridian/schemas` (orders cancel/executions/positions/snapshots; `accounts.reserved_cash`)
- `apps/web` routes / panels: existing order ticket; server actions `preview` / `create` / `cancel` (`app/actions/orders.ts`). No blotter UI (PBI-017).
- InsForge functions: `order-service` (`insforge/functions/order-service-src.ts`, bundle `order-service.ts`)
- Migrations: `insforge/migrations/0008_order-service.sql`; CLI twin `migrations/20260908214500_order-service.sql`

## Contracts

- Zod schemas (`@meridian/schemas`): `orderCancelRequestSchema`, `orderCancelResponseSchema`, `executionRecordSchema`, `positionRecordSchema`, `portfolioSnapshotSchema`; `orderRecordSchema.reserved_amount`; `accountSchema.reserved_cash`; preview `hours_outcome`
- Endpoints: `POST /functions/order-service/preview`; `POST /functions/order-service/orders`; `POST /functions/order-service/orders/:id/cancel`
- Records: `orders`, `executions` (append-only), `positions`, `portfolio_snapshots`, `accounts.reserved_cash`
- Events / realtime: channel `orders:{userId}`, event `order` via `publish_order_event`
- Generated reference: [generated/](../generated/) (after `pnpm docs:generate`)

## Rules

- Decision tables evaluated or seeded: DT-VAL-01, DT-RISK-01, DT-HRS-01, DT-FEE-01
- Entitlements / `authorize()`: `trade:preview`, `trade:create`, `trade:cancel` (authenticated-only helper; DT-ENT-01 still PBI-024)
- Mutating: create and cancel write `audit_log`; preview does not. Reject and accept paths persist `rule_audit_id` from `evaluateDomain`.

## How to extend

- PBI-015 matching-runner should call `assertTransition` / `canTransition` and `release_buying_power` on fills — do not fork a second FSM or reserve path
- New order types belong in `orderDraftSchema` + DT-VAL rows, not in the ticket
- Session is a fact (`open` / `closed` from NYSE calendar); do not copy hours into application code
- Rebuild `pnpm functions:bundle:order-service` after pipeline changes

## Ops

- Env vars (names only): `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, `INSFORGE_INTERNAL_URL` / `INSFORGE_BASE_URL`, `E2E_AUTH_STUB`
- Seed / fixtures: paper cash from account row; buying power = cash minus `reserved_cash`
- Test harness notes: Vitest `@meridian/paper-engine` `fsm.test.ts` (TC-014-01), `buying-power.test.ts` (TC-014-02), `order-pipeline.test.ts` (TC-014-03/04). Stub previews default `session: "open"`. Apply 0008 via `npx -y @insforge/cli db migrations up 20260908214500_order-service.sql`

## Trace

- Commit: `feat(PBI-014): order service validation pipeline and buying-power reserve`
- SHA: this commit
